die app stellt viele funktionalitäten von tmap dar. 
im prinzip kann man überall mit sachen rumspielen und sieht wie sich dadurch der code und die grafik ändert.
so lange man nichts sinnfreies eingibt, also gültige hex color codes bei eigener palette und zahlen als breaks verwendet sollte es keine crashes geben